// import axios from 'axios';
// import {CONVERT} from './types';

// const BACKEND_URI = "http://localhost:3001"

const sendForConversion = async () => {
    // FIGURE OUT HOW TO SEND A FILE
    // const res = await axios.post(`${BACKEND_URI}`);

    // dispatch(doSendForConversion(res.data))
};

// const doSendForConversion = (data) => {
//     return {type: CONVERT, payload: data}
// }

export { sendForConversion }